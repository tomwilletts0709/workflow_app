# Workflow

Workflow is an ongoing task-tracker project built in fastapi.

The app is currently focused on task management, including task models, service/repository layers, state transitions, pagination, search, WebSockets, background jobs, and PostgreSQL support.

There is a widening of scope as I progress through including the utilisation of AI agents within the project. 

## Status

This project is a work in progress.
